package co.edu.udec.model;

import java.util.UUID;

public class Operator extends UserData {
    public Operator(UUID uuid, String name, String address, int telephone) {
        super(uuid, name, address, telephone);
    }
}
